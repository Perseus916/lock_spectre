# Placeholder release keystore template
# Create your own release keystore locally (do not commit real keys):
# keytool -genkey -v -keystore release-key.jks -keyalg RSA -keysize 2048 -validity 10000 -alias release

